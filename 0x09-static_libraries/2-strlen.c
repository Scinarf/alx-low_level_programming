#include "main.h"
#include <string.h>

/**
  * _strlen - trying to return the length of a string
  * @s: contains the string in question
  * Return: The length of the string
  */

int _strlen(char *s)
{
	int a = strlen(s);

	return (a);

}
