#include "main.h"

/**
  * mul - This function is used to multiply two values
  * @a: this variable holds values one
  * @b: this variable holds value two
  * Return: 0
  */

int mul(int a, int b)
{
	int ans = (a * b);

	return (ans);
}
